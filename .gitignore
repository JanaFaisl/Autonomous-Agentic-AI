# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Streamlit
.streamlit/

# Evaluation scratch: regenerated by sbr_harness on every run.
# evaluation/runs/ is deliberately NOT ignored — those artifacts are the
# paper's raw data and belong in the artifact release.
evaluation/builds/

# ---- added when this directory became the repository root ----
# Archives: a multi-GB snapshot lives here; GitHub rejects files over 100 MB.
*.zip

# Agent tooling scratch, including Android emulator images (gigabytes).
.claude/

# Runtime database — recreated on first run.
app.db

# Test cache
.pytest_cache/

# Evaluation build scratch (node_modules, vite output), regenerated each run.
# evaluation/runs/ is deliberately NOT ignored: it is the paper's raw data.
evaluation/builds/
