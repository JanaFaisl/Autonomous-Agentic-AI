[pytest]
testpaths = tests
# Live-API suites are excluded by default so `pytest` is always cheap and fast.
# Opt in explicitly:  RUN_PIPELINE_EVAL=1 pytest -m slow
addopts = -m "not slow"
markers =
    slow: live-API tests that take minutes and cost money (run with '-m slow')
